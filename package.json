{
  "name": "member-qr-perks",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "esbuild": "^0.25.9",
    "qrcode": "^1.5.4"
  },
  "scripts": {
    "build": "esbuild node_modules/qrcode/lib/browser.js --bundle --minify --format=esm --outfile=public/qrcode.min.js"
  }
}
